{
  "name": "wplace-helper-utils",
  "version": "0.4.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && terser src/WplaceHelperUtils.user.js -o dist/WplaceHelperUtils.user.js --comments '/UserScript|@name|@description|@version|@match|@run-at|@grant|@updateURL|@downloadURL/' -c -m"
  },
  "devDependencies": {
    "terser": "^5.19.2"
  }
}
